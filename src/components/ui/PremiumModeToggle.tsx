'use client';

/**
 * Przełącznik Kupujący / Sprzedający / Partner wyłączony — aplikacja używa jednego konta.
 * Profil i pakiety Pro pozostają w /moje-konto i /cennik.
 */
export default function PremiumModeToggle(_props: { currentUser?: unknown }) {
  return null;
}
