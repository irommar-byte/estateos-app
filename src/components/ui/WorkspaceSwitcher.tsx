'use client';

/** Przełącznik trybu workspace wyłączony — parity z aplikacją mobilną. */
export default function WorkspaceSwitcher(_props: {
  currentMode: string;
  onModeChange: (mode: string) => void;
}) {
  return null;
}
