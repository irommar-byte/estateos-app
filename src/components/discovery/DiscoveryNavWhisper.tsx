import React from 'react';

/**
 * Ambient whispers demoted — IntelligencePulseTape is the single voice.
 */
export default function DiscoveryNavWhisper(_props: {
  navigation?: any;
  variant?: 'nav' | 'drawer';
  style?: object;
}) {
  return null;
}
