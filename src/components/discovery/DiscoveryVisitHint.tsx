import React from 'react';

/**
 * Soft visit hint demoted — IntelligencePulseTape is the single ambient voice.
 */
export default function DiscoveryVisitHint(_props: {
  navigation?: any;
  offerId: number | string;
  style?: object;
  isDark?: boolean;
}) {
  return null;
}
