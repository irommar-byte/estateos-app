"use client";

/**
 * Ambient whispers demoted — floating Intelligence pulse is the single voice.
 * Component kept so imports compile; always silent.
 */
export default function DiscoveryContactWhisper(_props: {
  beforeContact?: boolean;
  className?: string;
}) {
  return null;
}
