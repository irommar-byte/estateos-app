"use client";

/**
 * Soft visit hint demoted to silence by default — floating pulse owns ambient voice.
 * Kept as stub so offer pages keep compiling; enable later only for strong contradiction.
 */
export default function DiscoveryVisitHint(_props: {
  offerId: number | string;
  className?: string;
}) {
  return null;
}
