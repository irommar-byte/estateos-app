node_modules
.next
.env
*.log

# TypeScript incremental (regenerowany lokalnie)
*.tsbuildinfo

# kopie zapasowe / śmieci edytorów / łaty
*.bak
*.bak.*
*.BAK*
*.BACKUP*
*.backup*
*.BROKEN*
*.swp
*~

# archiwalia wyłączone przy deploy
app__DISABLED_*/
_archived_cleanup/
.hotfix_backup_*/

# Prisma — lokalne bazy SQLite, nie prod MySQL
prisma/*.db*
prisma/**/*.db*

# uploads serwowane przez app (binary)
public/uploads*

# Jednorazowe skrypty łatek w korzeniu projektu
fix_*.js
test_*.js
debug_*.js

# Przypadkowe grafiki testowe w korzeniu
/test.jpg
/test_out.jpg

# Notatki audytowe w korzeniu
audyt*.txt

.DS_Store

# lokalny stan deploy / rollback (SHA, znaczniki czasu)
.deploy/

# snapshoty artefaktów release (serwer; duże pliki)
releases/

# raport z automatycznego pipeline agenta (nie commituj)
scripts/.agent-deploy-report.txt
scripts/.release-report.txt
